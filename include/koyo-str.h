#ifdef FACEKID_H
#define FACEKID_H

char * to_upper(char * text);
char * to_lower(char * text);
int index(char s, char * text);
char * trim_left(int index, char * text);
char * trim_right(int index, char * text);

#endif